# 伪代码：道岔模组怎么运作（给人看的流程）

只描述流程与控制流，不含实现细节；需要核对实现时再去看 Java 代码。  

## 0. 参与的东西

* **道岔方块**（左开 / 右开）：状态只有一个关键量 —— 形状；有意义的只有两种：  
  * 直位：形状连接直股两端；  
  * 岔位：形状连接曲股与尖轨端。  
* **骑手按键**：服务端每 tick 都能读到「向左 / 向右」（原版输入包，无需自定义网络）。  
* **进路记录**（每辆车一条）：谁定的、是否已提交、什么时候过期。  

## 1. 每 tick 主循环（早于所有实体 tick）

```text
对每个已加载维度：
  1) 若维度没开新物理（minecart_improvements）→ 记一次警告并停用本维度逻辑
  2) 采样所有在线玩家的「向左 / 向右」（长按有效，松开后保留 0.5 秒记忆）
  3) 对每一辆矿车：
       不在轨上 / 速度≈0 / 不是新物理 → 跳过
       提前量 = max(2, 速度×2 + 2) 格
       沿轨道前走这些格（跟着弯道走）；每遇到一个道岔格 → 交给第 2 节
  4) 清掉过期的进路记录
```

## 2. 遇到道岔格时的判定顺序（顺序本身即规则）

```text
规则 1：车就在这个格上                 → 冻结，不动形状
规则 2：这辆车已提交进路（本 tick 必进格）→ 冻结
规则 3：形状被别的车锁着且已提交         → 不动；否则近者优先、同距岔位优先
规则 4：来向不属于本几何（格内转弯中间态）→ 不动
——— 以上都不成立，才是「可以拨岔」———
```

## 3. 可以拨岔时：按来向决定

```text
来向 = 车从哪一端进来（直股两端 F / T，或曲股 B）
  来自 T（直股另一端）→ 直位（本类型不响应这个方向）
  来自 F（尖轨端）    → 按了对应的键？岔位 : 直位
  来自 B（曲股，逆向）→ 岔位（自动汇入尖轨端；配置成 stop 则停车）
拨完：写方块形状 + 播拨岔音效 + 记下「这辆车定了这条进路」（并记是否已提交）
```

## 4. 形状怎么算（不硬编码形状名）

```text
已知两个方向 a、b → 扫所有铁轨形状，找出「出口方向恰好是 {a, b}」的那个平铺形状：
  直位 = 形状(直股两端)
  岔位 = 形状(曲股, 尖轨端)
```

## 5. 与原版铁轨的关系

```text
每次改形状后：通知相邻的原版铁轨重连（原版会把我们当铁轨并重算它自己的形状），
但原版工具在「3 个连接」时会按自己的优先级改回弯道
→ 所以通知完必须把目标形状写回。
```

## 6. 玩家视角的因果

```text
提前按住 → 形状在车到达前一 tick 定好 → 原版物理沿形状把车带走（速度不变）
没按 / 空车 / 非玩家骑手 → 直位 → 直行
车已经在格上 → 按什么都不再改（真实道岔：上了尖轨不能扳）
```
